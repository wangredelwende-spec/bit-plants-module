# Mockup (UX Prototype)

This folder contains a throwaway UX prototype consisting of 8 screens with static, hardcoded data.
It is **NOT** connected to the backend API or database, and it is **NOT** meant to evolve into the production frontend.
A future, real `frontend/` folder will be built separately when it is time to wire up real data.
